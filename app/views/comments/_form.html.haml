- if @comment && @comment.errors.any?
  %ul
    - @comment.errors.full_messages.each do |msg|
      %li
        = msg
= simple_form_for [answer, (@comment || Comment.new)], html: { class: "form-horizontal" }, remote: true do |f|
  = f.input :body
  = f.submit class: "btn btn-info btn-sm"